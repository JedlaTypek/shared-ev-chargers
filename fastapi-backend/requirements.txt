# === FastAPI & server ===
fastapi==0.115.0
uvicorn[standard]==0.30.1

# === Databáze ===
sqlalchemy==2.0.32
psycopg2-binary==2.9.9          # PostgreSQL driver
aiosqlite==0.19.0               # async SQLite (pro vývoj)

# === Konfigurace a prostředí ===
python-dotenv==1.0.1
pydantic-settings==2.4.0

# === Autentizace a bezpečnost ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# === Cache / sessions ===
redis==5.0.4

# === Migrace DB ===
alembic==1.13.2

# === Testování (volitelné) ===
pytest==8.3.3
httpx==0.27.2
